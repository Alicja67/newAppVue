import AddTitle from '../components/AddTitle.vue';
import { shallowMount } from '@vue/test-utils';

describe('Component AddTitle', () => {
  it('should render without crashing', () => {
    
  });
});